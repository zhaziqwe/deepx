#ifndef DEEPX_SHAPE_COMBINATION_HPP
#define DEEPX_SHAPE_COMBINATION_HPP

#include <vector>

namespace deepx
{
    std::vector<std::vector<int>> combination(int n, int k);
    std::vector<std::vector<int>> combination(int n);
}
#endif