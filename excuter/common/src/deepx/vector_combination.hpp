#ifndef DEEPX_VECTOR_COMBINATION_HPP
#define DEEPX_VECTOR_COMBINATION_HPP

#include <vector>

namespace deepx
{
    using namespace std;
    vector<vector<int>> combination(int n, int k);
    vector<vector<int>> combination(int n);
    vector<int> arrange(int n);
}
#endif